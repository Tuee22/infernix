# Infernix Legacy Tracking For Deletion

**Status**: Authoritative source
**Referenced by**: [README.md](README.md), [development_plan_standards.md](development_plan_standards.md)

> **Purpose**: Provide the explicit ledger of obsolete paths, duplicate guidance, and future
> cleanup work in `infernix`.

## Scope

- this ledger tracks implementation placeholders, compatibility shims, duplicate definitions, and
  stale guidance that still exists in the worktree or tracked index
- ordinary UI placeholder copy is not tracked here unless it preserves a fallback behavior or
  masks a live platform failure

## Pending Removal

| Location | Why it is slated for removal | Owning phase or sprint |
|----------|------------------------------|------------------------|
| Legacy stateless-workbench framing in `web/src/Infernix/Web/Workbench.purs`, `web/playwright/inference.spec.js`, `web/test/Main.purs`, `web/test/run_playwright_matrix.mjs`, `web/src/Main.purs`, `web/src/index.html`, and the matching `infernix-demo` request/response handlers wired to manual inference | The durable-context Chat surface in Phase 7 replaces the single-form manual workbench; per the dev-plan continuous-narrative rule the supported PureScript demo surface closes around durable contexts rather than a parallel HTTP fallback. | Phase 7 |
| Hardcoded `persistent://public/default/...` Pulsar topic URLs in `src/Infernix/Models.hs` and `src/Infernix/Types.hs`, the matching integration assertions in `test/integration/Spec.hs`, the PureScript expectations in `web/test/Main.purs`, and the `tenant: public` / `namespace: default` defaults in `chart/values.yaml` (`INFERNIX_PULSAR_TENANT` / `INFERNIX_PULSAR_NAMESPACE` env injection in `chart/templates/deployment-service.yaml` and `chart/templates/deployment-demo.yaml`) | The supported default Pulsar tenant/namespace is `infernix/demo`. The plan and governed docs now describe `persistent://infernix/demo/...` as the supported contract; the implementation should consume `INFERNIX_PULSAR_TENANT` / `INFERNIX_PULSAR_NAMESPACE` to compute topic names, default chart values should advertise `infernix` and `demo`, and `cluster up` must reconcile that tenant and namespace through the Pulsar admin API before topic creation. | Phase 7 |
| `chart/templates/deployment-service.yaml` plus its associated `service.*` stanza in `chart/values.yaml` and the `chart/templates/persistentvolumeclaim-service-data.yaml` PVC | The supported three-role daemon model in [../documents/architecture/daemon_topology.md](../documents/architecture/daemon_topology.md) splits the fused `infernix-service` Deployment into `chart/templates/deployment-coordinator.yaml` (stateless coordinator, replicas ≥ 2, soft anti-affinity, demo-gated) and `chart/templates/deployment-engine.yaml` (stateful engine, replicas ≤ #engine-capable-nodes, **required** one-per-node anti-affinity). The coordinator inherits the PVC for Pulsar subscription cursor durability; the engine is stateless on disk. | Phase 7 Sprint 7.7 (Coordinator/Engine Pod Split and HA Chart) |
| `Infernix.Demo.ResultBridge` planned module under `src/Infernix/Demo/ResultBridge.hs` (named in current Phase 7 Sprint 7.7) | The result-bridge is pure Pulsar coordination (read `inference.result.<mode>`, write back to the conversation topic) and is product-agnostic. It belongs in the shared library at `src/Infernix/Bridge/Result.hs` so the coordinator role loads it without taking a dependency on any `<appNamespace>.*`; future apps reuse it verbatim. The renamed module lands together with the daemon split in Phase 7 Sprint 7.7. | Phase 7 Sprint 7.7 (Coordinator/Engine Pod Split and HA Chart) |
| `daemonConfigRole`-keyed `cluster` and `host` strings in `src/Infernix/DemoConfig.hs` plus the matching `dhall/InfernixSubstrate.dhall` literals; `clusterDaemon` and `hostDaemon` field names in the generated `.dhall` (carried by `clusterDaemonConfig` and `hostDaemonConfig`); the `runtimeMode == AppleSilicon && daemonConfigRole daemonConfig == ClusterDaemon` conditional at `src/Infernix/Runtime/Pulsar.hs:574-590` | The supported daemon-role vocabulary is `coordinator` and `engine`, with `inference.batch.<mode>` as the supported handoff topic on every substrate. The conditional must generalize to forward whenever `daemonConfigHostBatchTopic` is set, irrespective of substrate. | Phase 7 Sprint 7.7 (Truly Stateless Daemon Topology and HA Chart) |
| `./.data/object-store/` tree (subtrees `artifacts/<substrate>/<model-id>/bundle.json`, `source-artifacts/<substrate>/<model-id>/source.json`, `manifests/<substrate>/<model-id>/default.pb`, `results/<requestId>.txt`) plus `objectStoreRoot` plumbing in `src/Infernix/Runtime/Cache.hs` and related helpers (`durableArtifactPathFor`, `sourceManifestPathFor`, `cacheManifestProtoPath`) | The supported target shape places all durable binary state in MinIO. Model weights move to `infernix-models`; user uploads and engine-generated artifacts move to `infernix-demo-objects`; cache rebuildability comes from the Pulsar conversation log via `prefixHash`. No durable host filesystem path remains. | Phase 7 Sprint 7.7 (Truly Stateless Daemon Topology and HA Chart) |
| `s3://infernix-runtime/...` URI scheme and the `localPathFromUri` mapping in `src/Infernix/Runtime/Cache.hs` (lines 177-184) that resolves the fake-S3 URI to a host filesystem path | The scheme was a placeholder that pretended to be S3 but resolved to local filesystem. With the new model-loader pulling real bytes from MinIO via `infernix-models`, the fake scheme has no remaining purpose. | Phase 7 Sprint 7.7 (Truly Stateless Daemon Topology and HA Chart) |
| 80-character inline-payload threshold in `src/Infernix/Runtime.hs:77` (`Text.length outputText > 80`) and the associated `buildPayload` branch that writes large outputs to `./.data/object-store/results/` | The threshold is a development-pass placeholder, not a designed value. Text outputs always ride inline in the Pulsar result message; binary outputs go directly to `infernix-demo-objects` via the engine adapter and carry an `ObjectRef`. | Phase 7 Sprint 7.7 (Truly Stateless Daemon Topology and HA Chart) |
| `/objects/:objectRef` HTTP route handler in `src/Infernix/Demo/Api.hs:231-240` (`serveObject`); the matching route entry in `src/Infernix/Routes.hs`; the registered route surface in `documents/reference/api_surface.md`, `documents/reference/web_portal_surface.md`, and `DEVELOPMENT_PLAN/system-components.md` | The browser fetches engine-generated artifacts via presigned GET URLs from MinIO directly (the existing Phase 7 `/api/objects` path), never through the demo backend. The `/objects/:objectRef` route is no longer the supported surface. | Phase 7 Sprint 7.7 (Truly Stateless Daemon Topology and HA Chart) |
| `infernix-runtime` and `infernix-results` bucket entries in `chart/values.yaml` plus the matching `INFERNIX_MINIO_RUNTIME_BUCKET` and `INFERNIX_MINIO_RESULTS_BUCKET` environment injection in `chart/templates/deployment-service.yaml` | Both buckets were chart-reserved placeholders that never wired to real MinIO. The supported target shape uses two buckets: `infernix-models` (always-on, platform model weights) and `infernix-demo-objects` (demo-gated, user uploads + engine-generated artifacts). | Phase 7 Sprint 7.7 (Truly Stateless Daemon Topology and HA Chart) |
| Every daemon PVC, including the previously-listed `persistentvolumeclaim-service-data.yaml` and the previously-planned `persistentvolumeclaim-coordinator-data.yaml` | The supported target shape places all durable state in MinIO and Pulsar. The engine pod's model cache is ephemeral `emptyDir` with `sizeLimit`; the coordinator's Pulsar subscription cursors are broker-side durable and need no local PVC. | Phase 7 Sprint 7.7 (Truly Stateless Daemon Topology and HA Chart) |

## Completed

| Location | Why it was slated for removal | Owning phase or sprint |
|----------|-------------------------------|------------------------|
| `/api/inference` `POST` and `GET /api/inference/<requestId>` handlers in `src/Infernix/Demo/Api.hs`, the matching CLI helper in `src/Infernix/CLI.hs`, the `/api` route purpose string in `src/Infernix/Routes.hs`, and the integration-test paths that exercised that surface in `test/integration/Spec.hs` | The supported demo manual-inference path closes through the durable-context WebSocket surface introduced by Phase 7. The legacy `/api/inference` stateless surface is fully removed from the Haskell sources (verified by repository-wide grep). | Phase 7 |
| `proto/infernix/api/inference_service.proto`, the matching enforcement entry in `src/Infernix/Lint/Proto.hs`, the generated `tools/generated_proto/infernix/api/inference_service_pb2.py` stub owned by `src/Infernix/Python.hs`, the `proto/README.md` listing, and the stale `docker/linux-substrate.Dockerfile` reference that still tried to compile the deleted proto file. | The gRPC service signature only described the legacy direct manual-inference HTTP API. The proto file was deleted in commit `cf88200` ("demo refactor"); the Dockerfile reference was the last stale fragment and was removed under Phase 7 together with the rest of the durable-context rollout. | Phase 7 |
| Apple service-disable logic and tests in `src/Infernix/Cluster.hs`, `src/Infernix/Models.hs`, `test/unit/Spec.hs`, plus plan and governed-doc wording that said Apple `cluster up` did not deploy `infernix-service` | The clarified final contract always runs cluster `infernix service` daemons. Apple differs by sending inference batches to same-binary host daemons through Pulsar, not by omitting the cluster daemon. | Phase 6 Sprint 6.25 |
| Publication and browser metadata that used `daemonLocation: control-plane-host` as the Apple service location in `src/Infernix/Models.hs`, `web/src/Infernix/Web/Workbench.purs`, `test/integration/Spec.hs`, and route/API docs | The final publication contract distinguishes cluster daemon location from inference executor location so readers do not infer that Apple lacks a cluster daemon. | Phase 6 Sprint 6.25 |
| Banner-prefixed JSON payloads written to `infernix-substrate.dhall` by `src/Infernix/Models.hs` and decoded through Aeson in `src/Infernix/DemoConfig.hs` and `src/Infernix/Config.hs` | The supported staged-substrate contract now uses a real typed Dhall record at the retained `infernix-substrate.dhall` filename, with schema documentation at `dhall/InfernixSubstrate.dhall` and in-process decoding through the `dhall` Haskell library. | Phase 6 Sprint 6.27 |
| `HASKELL_CLI_TOOL.md` | The tracked root-level Haskell CLI doctrine note duplicated governed guidance and named imported practices that are not part of the supported `infernix` contract, including docs-generation command surfaces and Pulumi infrastructure tests; the governed `documents/` suite, command registry, Haskell style guide, and explicit non-adoption rules are the supported homes instead | Phase 6 Sprint 6.13; Phase 6 Sprint 6.16 |
| Cluster-resident Apple inference parity in `src/Infernix/Cluster.hs`, `chart/templates/deployment-service.yaml`, `chart/templates/deployment-demo.yaml`, and the Apple route or publication docs that treated `infernix-service` in Kind as the canonical Apple inference executor | The final Apple doctrine keeps inference execution and result publication host-native for Apple GPU and unified-memory access while allowing cluster `infernix service` daemons to own request-topic consumption and host-batch handoff, so the old in-cluster Apple inference path had to be retired explicitly | Phase 3 Sprint 3.9; Phase 4 Sprint 4.3; Phase 4 Sprint 4.11; Phase 6 Sprint 6.19 |
| `--runtime-mode` parsing and `INFERNIX_RUNTIME_MODE` handling across `src/Infernix/CLI.hs`, `src/Infernix/Config.hs`, tests, and governed docs | The generated substrate `.dhall` beside the binary is the single source of truth for active substrate, so user-selected runtime overrides are obsolete in the final contract | Phase 0 Sprint 0.8; Phase 1 Sprint 1.10; Phase 4 Sprint 4.12; Phase 6 Sprint 6.19 |
| `INFERNIX_SUBSTRATE_ID` fallback in `src/Infernix/Config.hs`, `docker/linux-substrate.Dockerfile`, `web/test/run_playwright_matrix.mjs`, `src/Infernix/CLI.hs`, and `python/adapters/common.py` | The staged substrate file is the supported substrate selector, so the env fallback was an obsolete second selection path whenever that file was absent | Phase 1 Sprint 1.10; Phase 4 Sprint 4.12; Phase 6 Sprint 6.19 |
| `src/Infernix/Demo/Api.hs` direct placeholder handlers for `/harbor`, `/minio/*`, and `/pulsar/*`, plus the matching `rewrittenPath` acceptance in `test/integration/Spec.hs` and route-validation docs | The Gateway-owned edge contract should prove real Harbor, MinIO, and Pulsar upstream behavior on the supported path; compatibility placeholder payloads let validation pass without that stronger guarantee until the route hardening landed | Phase 6 Sprint 6.19 |
| Runtime-specific in-cluster demo-config filenames such as `infernix-demo-linux-cpu.dhall` emitted from `src/Infernix/Cluster.hs` and mounted by `chart/templates/deployment-*.yaml` | The repo-local mirror and plan treat `infernix-substrate.dhall` as the canonical staged filename, so the runtime-specific cluster key or mount name was a publication divergence that had to be removed explicitly | Phase 2 Sprint 2.9; Phase 3 Sprint 3.7 |
| Hardcoded `demoUiEnabled = True` in `src/Infernix/DemoConfig.hs` auto-generated substrate files | The codebase could honor `demo_ui = false` when given an explicit staged file, but the supported generation path forced the demo surface on until explicit materialization support for `--demo-ui false` landed | Phase 5 Sprint 5.1; Phase 6 Sprint 6.19 |
| Direct host `infernix-demo serve` launch guidance and the old host-demo-bridge route publication language | The supported Apple topology keeps the demo app cluster-resident when enabled, so the retired host-side demo HTTP host is no longer part of the routed contract even though Apple inference itself remains host-native | Phase 0 Sprint 0.8; Phase 3 Sprint 3.9; Phase 5 Sprint 5.8 |
| User-facing direct `linux-gpu` launcher guidance and any requirement for the outer control-plane container itself to request the NVIDIA runtime | Supported Linux control-plane commands must run through Compose, and the outer container does not need direct CUDA access even when the deployed substrate is GPU-backed | Phase 0 Sprint 0.8; Phase 1 Sprint 1.10; Phase 2 Sprint 2.9 |
| `linux-cuda` substrate naming across code, Kind assets, tests, and docs | The final canonical substrate id is `linux-gpu`, so the old `linux-cuda` label is legacy naming that must be retired explicitly | Phase 0 Sprint 0.8; Phase 1 Sprint 1.10; Phase 2 Sprint 2.9; Phase 6 Sprint 6.19 |
| Simulated cluster bring-up fallback behavior | The supported operator contract requires real Kind lifecycle on supported entrypoints; the only broadly portable lane is the real `linux-cpu` outer-container workflow rather than a simulated cluster surrogate | Phase 0 Sprint 0.8; Phase 4 Sprint 4.12; Phase 6 Sprint 6.19 |
| Legacy `default.state` cache-manifest fallback in `src/Infernix/Runtime/Cache.hs` | The supported durable cache-manifest contract is protobuf-backed `default.pb` state only, so the old text-state fallback kept an obsolete cache-manifest format alive in code | Phase 6 Sprint 6.17 |
| Legacy inference-result fallback in `src/Infernix/Runtime.hs` that read `./.data/runtime/results/*.state` when the protobuf result file was absent | The supported durable result contract is protobuf-backed `*.pb` state only, so the old text-state fallback kept an obsolete result format alive in code | Phase 6 Sprint 6.17 |
| Legacy generated-contract cleanup in `src/Infernix/CLI.hs` that deleted `web/src/Infernix/Web/Contracts.purs` during PureScript contract generation | The supported handwritten Haskell contract home moved to `src/Infernix/Web/Contracts.hs`, and generated PureScript output now belongs only under `web/src/Generated/`, so the retired output path should stop participating in supported builds | Phase 6 Sprint 6.17 |
| Legacy helper-registry cleanup shims in `src/Infernix/Cluster.hs` that removed the retired `infernix-bootstrap-registry` container and `./.build/kind/registry/localhost:30001` namespace | The supported Harbor-first bootstrap path no longer uses the helper registry at all, so the remaining cleanup code was the last compatibility surface from that retired bootstrap model | Phase 6 Sprint 6.17 |
| `chart/templates/service-api.yaml`, the production HTTP service port surface, and the `infernix service --port` compatibility flag | Production `infernix service` is now consistently a no-HTTP daemon on the supported path | Phase 4 Sprint 4.8 |
| `tools/service_server.py` and the old Python HTTP API host | The demo HTTP surface now lives in `src/Infernix/Demo/Api.hs` and is served by `infernix-demo` | Phase 3 Sprint 3.6; Phase 4 Sprint 4.4 |
| `tools/runtime_backend.py`, `tools/runtime_worker.py`, `tools/runtime_fixture_backend.py`, `tools/final_engine_runner.py` | The monolithic Python runtime helpers are gone; runtime work now closes through Haskell plus the adapter boundary | Phase 4 Sprint 4.2; Phase 4 Sprint 4.5 |
| `tools/demo_config.py`, `tools/discover_chart_images.py`, `tools/discover_chart_claims.py`, `tools/list_harbor_overlay_images.py`, `tools/helm_chart_check.py`, `tools/platform_asset_check.py`, `tools/proto_check.py`, `tools/lint_check.py`, `tools/docs_check.py`, `tools/haskell_style_check.py`, `tools/publish_chart_images.py`, `tools/requirements.txt` | Control-plane tooling is Haskell-owned and no build-time Python remains on the supported control-plane path | Phase 1 Sprint 1.6 |
| Harbor chart-managed standalone PostgreSQL deployment path | Harbor and later PostgreSQL-backed services now use operator-managed Patroni clusters only | Phase 3 Sprint 3.2; Phase 6 Sprint 6.7 |
| Bootstrap helper-registry path on `localhost:30001` | Harbor-first bootstrap now mirrors only the final Harbor-backed image flow and no longer needs the helper registry | Phase 2 Sprint 2.4 |
| `docker/linux-base.Dockerfile`, `docker/linux-cpu.Dockerfile`, `docker/linux-gpu.Dockerfile` | The supported Linux image story now closes through one shared `docker/linux-substrate.Dockerfile` that produces the two real Linux runtime images | Phase 4 Sprint 4.9 |
| Live repo-mount assumptions in `compose.yaml` (`.:/workspace`, `infernix-web-node-modules`, and the live source-edit launcher posture) | The Linux launcher now uses a baked image snapshot and bind-mounts only the supported host state surfaces: `./.data/`, `./.build/`, `./chart/charts/`, the host `compose.yaml`, and the Docker socket | Phase 1 Sprint 1.9; Phase 6 Sprint 6.21 |
| `infernix-build` and `infernix-cabal-home` named volumes in `compose.yaml` and the `INFERNIX_BUILD_ROOT=/opt/build/infernix` outer-container build root | Outer-container substrate-file staging now uses the host-anchored `./.build/outer-container/build/` path through a bind mount, while cabal-home and the cabal builddir stay at the toolchain's natural in-image locations, so docker-managed named volumes are no longer part of the supported launcher contract | Phase 1 Sprint 1.9 |
| `INFERNIX_CABAL_BUILDDIR` and `CABAL_DIR` overrides in `docker/linux-substrate.Dockerfile`, `compose.yaml`, the host-side CLI plumbing in `src/Infernix/Config.hs`, `src/Infernix/CLI.hs`, `test/integration/Spec.hs`, and `web/package.json`, plus the `./.build/outer-container/cabal-home/` and `./.build/outer-container/build/cabal/` paths under the bind mount, plus the brief `docker/launcher-entrypoint.sh` seed wrapper that the substrate image used as its `ENTRYPOINT` | The supported Linux outer-container path now lets cabal use its natural builddir and `CABAL_DIR` defaults inside the substrate image; cabal package state lives in the image overlay rather than in any bind-mounted host path, which removes the bind-mount-hides-baked-state failure mode that the launcher wrapper was working around, and the substrate image now uses `tini` as its entrypoint instead | Phase 1 Sprint 1.9 |
| Playwright fonts, gstreamer plugins, browser-runtime libs, xvfb, and the `playwright install chromium firefox webkit` step in `docker/linux-substrate.Dockerfile` | Playwright now ships as the dedicated `infernix-playwright:local` container built from `docker/playwright.Dockerfile`, so the substrate image stops carrying browser-runtime weight | Phase 5 Sprint 5.5 |
| `resolvePlaywrightImage`, the direct `docker run` Playwright invocation, and the host-native E2E fallback in `runEndToEnd` in `src/Infernix/CLI.hs`; `ensurePlaywrightBrowsers` and `platformCommandsAvailableForE2E` in `src/Infernix/Workflow.hs` | Routed Playwright execution now closes through `docker compose run --rm playwright` for every substrate, and Docker is a hard prerequisite for `infernix test e2e` | Phase 5 Sprint 5.8 |
| `web/package-lock.json` tracked under version control | The supported Node toolchain regenerates the lockfile during each image build through `npm install`, so the tracked lockfile became a duplicate source of truth that drifted from the resolution executed at image build time | Phase 5 Sprint 5.5 |
| `python/apple-silicon/`, `python/linux-cpu/`, and `python/linux-gpu/` project duplication | The supported Python boundary now closes through one `python/pyproject.toml` and one shared `python/adapters/` tree | Phase 4 Sprint 4.7 |
| `src/Generated/Contracts.hs` | The handwritten Haskell browser-contract source now lives under `src/Infernix/Web/Contracts.hs`; `Generated/` is reserved for generated output only | Phase 5 Sprint 5.7 |
| `chart/templates/httproutes/*.yaml`, route duplication in `src/Infernix/Models.hs`, and committed generated route or publication payload copies in `chart/values.yaml` | The supported route or publication contract now closes through one Haskell route registry, one data-driven HTTPRoute template, structural chart defaults only, and registry-backed route-doc plus route-lint derivation | Phase 2 Sprint 2.3; Phase 3 Sprint 3.8; Phase 6 Sprint 6.11 |
| `npx`-based Playwright workflow references in the supported CLI, docs, and web scripts | Supported Playwright workflows now use `npm --prefix web exec -- playwright ...` | Phase 1 Sprint 1.9; Phase 5 Sprint 5.5; Phase 6 Sprint 6.3 |
| Broad root-guidance drift across `README.md`, `AGENTS.md`, and `CLAUDE.md` | The governed metadata model, canonical-home links, and thin assistant-entry-doc posture are landed | Phase 1 Sprint 1.8; Phase 6 Sprint 6.9; Phase 6 Sprint 6.12 |
| Handwritten route inventory summaries in `README.md`, `documents/engineering/edge_routing.md`, `documents/reference/web_portal_surface.md`, `documents/tools/harbor.md`, `documents/tools/minio.md`, `documents/tools/pulsar.md`, and `documents/operations/cluster_bootstrap_runbook.md` | Route-oriented docs now consume registry-backed generated sections from the Haskell route registry instead of hand-maintained inventories | Phase 6 Sprint 6.11 |
| Phrase-based route-aware lint in `src/Infernix/Lint/Chart.hs` and `src/Infernix/Lint/Docs.hs` | Route-aware validation now checks registry-backed generated sections instead of relying on handwritten route phrases alone | Phase 6 Sprint 6.11 |
| Repeated assistant workflow guidance across `AGENTS.md` and `CLAUDE.md` without one canonical `documents/` assistant-workflow home | The root automation entry docs now stay thin and point at `documents/development/assistant_workflow.md` as the canonical repository-level assistant workflow home | Phase 6 Sprint 6.12 |
| Duplicated web-dependency readiness checks in `src/Infernix/Workflow.hs` and `src/Infernix/Cluster.hs` | The cluster path now reuses `Infernix.Workflow.ensureWebDependencies` instead of maintaining its own readiness probe | Phase 6 Sprint 6.12 |
| Dormant `victoria-metrics-k8s-stack` monitoring placeholder in `chart/values.yaml` | Monitoring is not a supported first-class surface, so the stale chart toggle was removed instead of implying a supported monitoring stack | Phase 6 Sprint 6.14 |
| Root `README.md` wording that described `linux-cpu` as a native-or-containerized lane | The supported runtime model is one Apple split-executor lane plus two containerized Linux lanes, so the remaining root-language drift item had to be removed to keep runtime honesty aligned across the plan and governed docs | Phase 6 Sprint 6.16 |
| Duplicate demo-config generated-banner literals in `src/Infernix/Workflow.hs` and `src/Infernix/DemoConfig.hs` | The shared workflow-helper closure was not complete while the demo-config banner constant still existed in parallel definitions | Phase 6 Sprint 6.16 |
| `documents/development/testing_strategy.md` metadata and purpose text that framed it as an authoritative canonical validation surface alongside `documents/engineering/testing.md` | The testing doctrine now closes through one canonical home, with `documents/development/testing_strategy.md` reduced to supporting operator detail instead of a competing authoritative source | Phase 6 Sprint 6.16 |

## Cross-References

- [README.md](README.md)
- [00-overview.md](00-overview.md)
- [development_plan_standards.md](development_plan_standards.md)
