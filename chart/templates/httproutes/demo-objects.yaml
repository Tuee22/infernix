{{- if .Values.demo.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: infernix-demo-objects
  namespace: platform
  annotations:
    infernix.io/purpose: Demo object store
spec:
  parentRefs:
    - name: infernix-edge
      sectionName: http
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /objects
      backendRefs:
        - name: infernix-demo
          port: 80
{{- end }}
