{{- if .Values.demo.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: infernix-demo-root
  namespace: platform
  annotations:
    infernix.io/purpose: Demo workbench
spec:
  parentRefs:
    - name: infernix-edge
      sectionName: http
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: infernix-demo
          port: 80
{{- end }}
