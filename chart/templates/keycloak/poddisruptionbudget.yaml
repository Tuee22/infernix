{{- if and .Values.demo.enabled .Values.keycloak.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: infernix-keycloak
  namespace: platform
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: infernix-keycloak
{{- end }}
