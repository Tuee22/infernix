apiVersion: v1
kind: Service
metadata:
  name: infernix-web
  namespace: platform
spec:
  selector:
    app.kubernetes.io/name: infernix-web
  ports:
    - name: http
      port: 80
      targetPort: {{ .Values.web.port }}
