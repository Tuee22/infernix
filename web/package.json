{
  "name": "infernix-web",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "generate:contracts": "sh -c 'set -eu; builddir=\"${INFERNIX_CABAL_BUILDDIR:-../.build/cabal}\"; cabal --builddir=\"$builddir\" run exe:infernix -- internal generate-purs-contracts ./src'",
    "build:purs": "sh -c 'set -eu; rm -rf dist; mkdir -p dist; cp src/index.html dist/index.html; npm exec -- spago build; npm exec -- spago bundle --module Main --outfile dist/app.js --platform browser --bundle-type app --force'",
    "build": "npm run generate:contracts && npm run build:purs",
    "test:unit": "npm run build && npm exec -- spago test",
    "test:e2e": "node ./test/run_playwright_matrix.mjs",
    "test:e2e:image": "npm exec -- playwright test ./playwright/inference.spec.js --reporter=list --timeout=30000"
  },
  "devDependencies": {
    "esbuild": "0.28.0",
    "playwright": "1.57.0",
    "purescript": "0.15.16",
    "spago": "0.93.44"
  }
}
